# Nomer as rotas para facilitar(ok)

# Criar funcionalidade de registro de click
    1 - Criar uma rota para receber a requisição(ok)
    2 - Fazer a requisição(ok)
    3 - Receber os dados(ok)
    4 - Mandar para o banco(ok)
    5 - Melhorar a forma de incrementação de um novo registro(ok)

# Fazer upload de imagens(ok)
# Adicionado dependência doctrine/dbal(ok)
# Alterar valor padrão da coluna `op_profile_image` da tabela Pages

# Criar funcionalidade de compartilhamento
